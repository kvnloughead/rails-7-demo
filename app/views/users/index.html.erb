<!-- View showing paginated users. @users is declared in the index action. -->

<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>
<ul class="users">
  <!-- Iterates through @users and fetches the _user partial for each one -->
  <%= render @users %>
</ul>
<%= will_paginate %>
